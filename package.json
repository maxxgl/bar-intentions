{
    "name": "bar-intentions",
    "version": "1.0.0",
    "main": "index.html",
    "scripts": {
        "serve": "browser-sync start --server --files \"css/*.css, js/*.js, *.html\"",
        "start": "npm run serve"
    },
    "devDependencies": {
        "browser-sync": "^2.18.8"
    }
}
